#ifndef _SELECT_H_
#define _SELECT_H_

#include <vector>
#include <random>

int randomized_select(std::vector<int> &, int);
int linear_select(std::vector<int> &, int);
bool check(std::vector<int> &, int, int);

#endif
